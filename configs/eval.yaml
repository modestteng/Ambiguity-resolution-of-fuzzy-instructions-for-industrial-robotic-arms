# Evaluation Configuration
model:
  name: "Qwen/Qwen2.5-VL-7B-Instruct"
  checkpoint_path: "data/outputs/checkpoints/best"

data:
  test_path: "data/raw/test.jsonl"
  max_length: 2048

eval:
  batch_size: 8
  metrics: ["accuracy", "f1", "bleu"]
  output_dir: "data/outputs/metrics"
  figures_dir: "data/outputs/figures"
  seeds: [42, 123, 456]
