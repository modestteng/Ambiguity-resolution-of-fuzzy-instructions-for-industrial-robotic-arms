# `my_paper_project` 结构说明

这个项目结构适合论文复现、模型训练、实验记录和论文写作并行推进。下面按模块解释每一个小块的用途。

## 项目总览

```text
my_paper_project/
├─ README.md
├─ requirements.txt
├─ .gitignore
├─ configs/
├─ data/
├─ src/
├─ experiments/
└─ paper/
```

---

## 1. 顶层文件

### `README.md`
- 项目的总说明文件。
- 一般写项目目标、研究问题、环境安装方法、训练与评测命令、目录结构说明。
- 新成员进入项目时，通常先看这个文件。

### `requirements.txt`
- Python 依赖列表。
- 用来记录运行项目需要安装哪些包，例如 `torch`、`transformers`、`numpy`。
- 方便别人用 `pip install -r requirements.txt` 快速搭环境。

### `.gitignore`
- Git 忽略规则文件。
- 指定哪些文件不要提交到仓库，例如模型权重、日志、缓存、临时文件。
- 常见忽略对象包括 `__pycache__/`、`.ipynb_checkpoints/`、大体积输出文件等。

---

## 2. `configs/`

这个文件夹用于存放配置文件。配置文件的作用是把“实验参数”从代码里拆出来，方便复现和修改。

### `configs/train.yaml`
- 训练配置文件。
- 一般包含：
  - 学习率
  - batch size
  - epoch 数
  - 模型名称
  - 数据路径
  - 输出路径
  - 随机种子
- 好处是不用每次改 Python 代码，就能切换不同训练设置。

### `configs/eval.yaml`
- 评测配置文件。
- 一般包含：
  - 测试集路径
  - checkpoint 路径
  - 评测指标
  - 输出结果保存位置
- 训练和评测分开配置，有助于管理不同实验阶段。

---

## 3. `data/`

这个文件夹用于统一管理数据和运行输出，避免项目根目录变乱。

### `data/raw/`
- 原始数据存放位置。
- 用来保存最初下载或收集到的数据，不建议直接修改。
- 这样可以保证原始数据始终可追溯，后续清洗出错时也能重新处理。

### `data/outputs/`
- 项目运行后产生的各种结果文件统一放在这里。
- 这是“实验产物”的集中目录。

#### `data/outputs/checkpoints/`
- 保存训练得到的模型权重。
- 例如某个 epoch 的模型参数、最佳验证集模型、最终模型。
- 后续评测、继续训练、做消融实验都会用到这些文件。

#### `data/outputs/logs/`
- 保存训练日志和运行日志。
- 例如 loss 变化、训练时间、显存占用、命令行输出。
- 出现训练异常时，这里通常是第一排查入口。

#### `data/outputs/metrics/`
- 保存数值评测结果。
- 例如 accuracy、F1、BLEU、success rate、消歧准确率等。
- 这些结果通常会进一步整理到论文表格中。

#### `data/outputs/figures/`
- 保存实验图像结果。
- 例如 loss 曲线、柱状图、混淆矩阵、消融结果图。
- 后续可以直接复制到论文或汇报 PPT 中。

---

## 4. `src/`

这个文件夹是核心源码区，负责真正的数据处理、模型定义、训练和评测逻辑。

### `src/dataset.py`
- 数据读取与预处理代码。
- 一般负责：
  - 读取原始数据文件
  - 清洗或格式转换
  - 构建训练样本
  - 返回模型可直接使用的数据格式
- 如果你换数据集，通常这里会首先改动。

### `src/model.py`
- 模型定义文件。
- 用来实现论文中的核心方法或 baseline 模型。
- 一般包含网络结构、前向传播逻辑、损失函数相关部分。
- 这是最接近“方法章节”的代码文件。

### `src/train.py`
- 训练入口文件。
- 一般负责：
  - 加载配置
  - 构建数据集和模型
  - 执行训练循环
  - 保存 checkpoint
  - 记录日志
- 通常运行训练时会执行这个脚本。

### `src/eval.py`
- 评测入口文件。
- 一般负责：
  - 加载训练好的模型
  - 在验证集或测试集上跑推理
  - 计算指标
  - 保存评测结果
- 论文中的主结果、对比实验、误差分析通常从这里产出。

### `src/utils.py`
- 工具函数文件。
- 存放多个脚本会重复使用的辅助代码，例如：
  - 随机种子设置
  - 日志初始化
  - 指标计算小函数
  - 文件读写辅助函数
- 这样可以减少重复代码，让主逻辑更清晰。

---

## 5. `experiments/`

这个文件夹用于记录实验方案和实验结论，是连接“代码”和“论文”的中间层。

### `experiments/exp01_baseline.md`
- baseline 实验记录。
- 用于说明最基础对比方法怎么做、参数如何设置、结果如何。
- 这是后续证明“你的方法有提升”的参考基线。

### `experiments/exp02_ablation.md`
- 消融实验记录。
- 用来分析模型中每个模块是否真的有效。
- 例如去掉上下文模块、去掉约束模块、去掉澄清策略后性能下降多少。
- 这是论文实验部分里很关键的一块。

### `experiments/exp03_final.md`
- 最终实验版本记录。
- 一般整理最终模型、最终超参数、最终结果和主要结论。
- 后续写论文时，这份文档会非常有用，因为它更接近投稿版本。

---

## 6. `paper/`

这个文件夹专门服务于论文写作，不和代码、日志混放。

### `paper/outline.md`
- 论文大纲。
- 用来先搭建章节结构，例如摘要、引言、相关工作、方法、实验、结论。
- 适合在正式写论文前先理清逻辑。

### `paper/related_work.md`
- 相关工作整理文件。
- 用来记录文献分类、已有方法、与你工作的差异点。
- 后续通常会发展成论文中的 Related Work 章节。

### `paper/figures/`
- 论文插图文件夹。
- 存放最终用于论文的图片，例如模型框架图、结果图、流程图。
- 和 `data/outputs/figures/` 的区别是：
  - `data/outputs/figures/` 更偏原始实验输出
  - `paper/figures/` 更偏经过筛选和整理后、可以直接放进论文的图

### `paper/tables/`
- 论文表格文件夹。
- 存放论文中使用的表格素材，例如主结果表、消融表、错误分析表。
- 适合把实验结果进一步整理成正式论文格式。

---

## 7. 这个结构为什么好用

- 代码、数据、实验记录、论文写作被分开管理，不容易混乱。
- 配置独立出来后，实验更容易复现。
- `experiments/` 让你不会只留下结果数字，却忘了当时是怎么跑出来的。
- `paper/` 单独存在，可以让“研究执行”和“论文写作”同步推进。

---

## 8. 一句话理解各模块

- `README.md`：项目说明书
- `requirements.txt`：环境依赖清单
- `.gitignore`：Git 忽略规则
- `configs/`：实验参数设置
- `data/`：数据和运行产物
- `src/`：核心代码
- `experiments/`：实验记录与分析
- `paper/`：论文写作材料

---

## 9. 适合你的使用方式

如果你是拿这个结构来做“机械臂模糊指令消歧”论文，可以这样对应：

- `configs/`：放训练和评测参数
- `data/raw/`：放原始指令数据、场景数据、标注数据
- `src/`：放消歧模型、数据处理、训练和评测代码
- `experiments/`：记录 baseline、消融、最终实验
- `paper/`：整理论文大纲、相关工作、图表和表格

这样做的好处是，后面从“跑实验”过渡到“写论文”会很顺，不需要重新整理材料。
